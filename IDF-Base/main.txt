┌──────────────────────────────────────────────────────────────┐
│                        app_main()                             │
└───────────────┬──────────────────────────────────────────────┘
                │
                ▼
      ┌──────────────────────────┐
      │ xTaskCreate(app_task)    │
      └───────────────┬──────────┘
                      │
                      ▼
================================================================
=                    FASE 0 — INFRAESTRUCTURA                  =
================================================================
│ littlefs_init()
│ init_logger_system()
│ init_system_events()
│ setenv(TZ), tzset()
│ log versión
└───────────────────────────────────────────────────────────────►

================================================================
=                    FASE 1 — NVS + wifi.json                  =
================================================================
│ nvs_flash_init()
│ si wifi.json no existe → crear archivo inicial
└───────────────────────────────────────────────────────────────►

================================================================
=                    FASE 2 — RED + WIFI                       =
================================================================
│ esp_netif_init()
│ esp_event_loop_create_default()
│ wifi_hardware_init()
│ xTaskCreate(wifi_check_task)
│ xTaskCreate(wifi_connect_task)
└───────────────────────────────────────────────────────────────►

================================================================
=                FASE 3 — SISTEMA DE COMANDOS                  =
================================================================
│ command_queue = xQueueCreate()
│ dispatcher.start()
│ registrar comandos (incluye "bomba")
│ xTaskCreate(dispatcher_task)
│ cargar_config_desde_file()
└───────────────────────────────────────────────────────────────►

================================================================
=                FASE 4 — CONEXIÓN MAESTRA                     =
================================================================
│ iniciar_proceso_conexion_maestra()
└───────────────────────────────────────────────────────────────►

================================================================
=                FASE 5 — NTP (si hay WiFi)                    =
================================================================
│ ntp_client_init()
│ esperar sincronización
└───────────────────────────────────────────────────────────────►

================================================================
=                FASE 6 — OTA                                  =
================================================================
│ preparar URL OTA
│ ota_worker_start()
│ si hay WiFi → process_commands("ota")
└───────────────────────────────────────────────────────────────►

================================================================
=        FASE 7 — SCHEDULER + SERVICIOS DEL PROYECTO           =
================================================================
│ miTS_init()
│ registrar task_60 / task_3600_post / task_3600_updt
│
│ while (g_ota_en_progreso):
│     esperar 2s
│
│ start_scheduler()
│ xTaskCreate(service_starter_task)
└───────────────────────────────────────────────────────────────►

================================================================
=                service_starter_task()                         =
================================================================
│ esperar fin de OTA o timeout
│ si WiFi listo y no OTA:
│     iniciar MQTT
│     iniciar WebServer
│     crear tareas auxiliares (NTP-check)
│     ejecutar FIRST RUN:
│         task_60()
│         task_3600_post()
│         task_3600_updt()
│ loop cada 2s
└───────────────────────────────────────────────────────────────►

================================================================
=                    LOOP PRINCIPAL                             =
================================================================
│ cada 60s:
│     log heap
│     ejecutar tareas especiales del proyecto
└───────────────────────────────────────────────────────────────►
